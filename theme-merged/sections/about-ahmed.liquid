<section class="about-ahmed-section">
  <div class="container">
    <div class="about-ahmed-grid">
      <div class="ahmed-profile">
        {% if section.settings.ahmed_image %}
          <img src="{{ section.settings.ahmed_image | image_url: width: 400 }}" 
               alt="Ahmed" class="ahmed-image">
        {% endif %}
        <div class="ahmed-bio">
          <h2>{{ section.settings.title }}</h2>
          <div class="ahmed-description">
            {{ section.settings.description }}
          </div>
        </div>
      </div>
      
      <div class="credentials-grid">
        {% for block in section.blocks %}
          {% case block.type %}
            {% when 'credential' %}
              <div class="credential-item">
                <i class="{{ block.settings.icon }}"></i>
                <h3>{{ block.settings.title }}</h3>
                <p>{{ block.settings.description }}</p>
              </div>
          {% endcase %}
        {% endfor %}
      </div>
    </div>
  </div>
</section>

{% schema %}
{
  "name": "About Ahmed",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Section Title",
      "default": "About Ahmed"
    },
    {
      "type": "image_picker",
      "id": "ahmed_image",
      "label": "Ahmed's Photo"
    },
    {
      "type": "richtext",
      "id": "description",
      "label": "Bio Description",
      "default": "<p>Ahmed is currently a student at NJIT, where he focuses on computing and information systems coursework while balancing his studies with community projects. He brings a hands-on approach to learning and enjoys applying technical skills to build tools and experiences for other collectors.</p>\n<p>He's been an enthusiastic Pokémon fan since the release of Pokémon Black & White, and over the last two years has grown a binder-style collection that he curates carefully. Ahmed specializes in organizing sets by rarity and condition, and he documents his collection on PokePage to help other collectors discover and trade cards.</p>\n<p>Outside of collecting, Ahmed organizes local meetups, participates in online communities, and is committed to making PokePage a welcoming place for both new and experienced collectors. If you'd like to connect or trade, check the newsletter and events sections for upcoming meetups.</p>"
    }
  ],
  "blocks": [
    {
      "type": "credential",
      "name": "Credential",
      "settings": [
        {
          "type": "text",
          "id": "icon",
          "label": "Font Awesome Icon Class",
          "default": "fas fa-graduation-cap"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Credential Title"
        },
        {
          "type": "text",
          "id": "description",
          "label": "Credential Description"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "About Ahmed"
    }
  ]
}
{% endschema %}
